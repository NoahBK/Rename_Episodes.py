# Rename_Episodes.py
The script scans all video files in the folder, checks their metadata titles, and renames files to match exactly, ensuring consistent naming. It handles special cases like ellipses so episode titles in filenames reflect the metadata accurately.
